<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= subject %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
        background-color: #f5f5f5;
        color: #222;
        margin: 0;
        padding: 0;
      }
      .email-container {
        max-width: 650px;
        margin: 30px auto;
        background: #fff;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.06);
      }
      .email-header {
        padding: 25px;
        text-align: center;
        border-bottom: 1px solid #eee;
      }
      .email-header img {
        height: 42px;
      }
      .email-body {
        padding: 40px 35px;
      }
      h2 {
        font-weight: 600;
        margin-bottom: 20px;
        font-size: 22px;
        color: #222;
      }
      p {
        font-size: 15px;
        line-height: 1.6;
        color: #555;
      }
      .highlight-card {
        background: #fafafa;
        border-radius: 8px;
        padding: 18px 20px;
        border-left: 3px solid #cc0000;
        margin: 25px 0;
      }
      .info-box {
        background: #fafafa;
        border-radius: 8px;
        padding: 15px;
        border: 1px solid #eee;
      }
      .btn-primary {
        background: #cc0000;
        border: none;
        padding: 12px 28px;
        font-weight: 600;
        font-size: 15px;
        border-radius: 25px;
        transition: all 0.3s ease;
        text-decoration: none;
        color: #fff !important;
        display: inline-block;
      }
      .btn-primary:hover {
        background: #a30000;
        transform: translateY(-2px);
        box-shadow: 0 4px 10px rgba(204, 0, 0, 0.2);
      }
      .divider {
        height: 1px;
        background: #eee;
        margin: 35px 0;
      }
      .email-footer {
        background: #fafafa;
        padding: 25px 30px;
        font-size: 12px;
        text-align: center;
        color: #888;
        border-top: 1px solid #eee;
      }
      .disclaimer {
        margin-top: 15px;
        font-size: 11px;
        line-height: 1.5;
        color: #999;
      }
    </style>
  </head>
  <body>
    <div class="email-container">
      <!-- Header -->
      <div class="email-header">
        <img src="<%= logoUrl %>" alt="<%= siteName %>" />
      </div>

      <!-- Body -->
      <div class="email-body">
        <h2 class="text-center">Hello <%= toName %>,</h2>

        <p class="text-center">
          <% if (message) { %> <%- message.replace(/\n/g, '<br />') %> <% } else
          { %> You have an important notification from
          <strong><%= siteName %></strong>. <% } %>
        </p>

        <!-- Highlight Card -->
        <div class="highlight-card">
          <div class="row">
            <div class="col-4">
              <strong style="color: #cc0000">Subject</strong>
            </div>
            <div class="col-8"><%= subject %></div>
          </div>
        </div>

        <!-- Info Row -->
        <div class="row mb-4">
          <div class="col-md-6 mb-3">
            <div class="info-box">
              <h6 style="color: #cc0000; font-weight: 600; margin-bottom: 6px">
                Priority
              </h6>
              <p class="mb-0">Important Notification</p>
            </div>
          </div>
          <div class="col-md-6 mb-3">
            <div class="info-box">
              <h6 style="color: #cc0000; font-weight: 600; margin-bottom: 6px">
                Reference
              </h6>
              <p class="mb-0">#<%= Math.floor(Math.random() * 10000) %></p>
            </div>
          </div>
        </div>

        <div class="divider"></div>

        <!-- CTA -->
        <div class="text-center mb-4">
          <a href="<%= siteUrl %>" class="btn-primary">View Details</a>
        </div>

        <p class="text-center" style="font-size: 13px; color: #777">
          For security, don’t share this email.<br />
          Questions? Contact support from your dashboard.
        </p>

        <div
          class="text-center mt-4 pt-3 border-top"
          style="border-color: #eee !important"
        >
          <p class="mb-0" style="font-size: 13px">
            Kind regards,<br />
            <strong style="color: #cc0000"><%= siteName %> Team</strong>
          </p>
        </div>
      </div>

      <!-- Footer -->
      <div class="email-footer">
        <div class="mb-2">
          <strong><%= siteName %></strong> | Authorized EMI
        </div>
        <div class="mb-2" style="font-size: 11px">
          Regulated by FCA | Member of Financial Ombudsman Service
        </div>

        <% if (includeDisclaimer) { %>
        <div class="disclaimer">
          Confidentiality: This email is intended only for the recipient. This
          is not financial or legal advice. <%= siteName %> accepts no liability
          for misuse.
        </div>
        <% } %>

        <p style="margin-top: 12px; font-size: 11px; color: #aaa">
          © <%= new Date().getFullYear() %> <%= siteName %>. All rights
          reserved.
        </p>
      </div>
    </div>
  </body>
</html>
