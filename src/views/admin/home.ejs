<h1 class="mb-4">My Links</h1>
<div class="container">
  <div class="row">
    <% for (let link of links) { %>
    <div class="col-sm-6 col-md-5 col-lg-4 mb-4">
      <div class="card h-100 shadow-sm">
        <div class="card-body">
          <h5 class="card-title"><%= link.name %></h5>
          <p class="card-text text-muted">
            Created: <%= link.createdAt.toLocaleString() %>
          </p>
        </div>
        <div class="card-footer bg-transparent border-top-0">
          <div class="row g-2 w-100">
            <div class="col-6">
              <a
                href="<%= link.link %>"
                target="_blank"
                class="btn btn-primary btn-sm w-100"
              >
                Visit Link
              </a>
            </div>
            <div class="col-6">
              <!-- <button
                type="button"
                class="btn btn-warning btn-sm w-100"
                data-bs-toggle="modal"
                data-bs-target="#paymentModal"
                onclick="loadPaymentDetails('<%= link.transactionId %>')"
              >
                View Details
              </button> -->
              <a
                href="/link-info/<%= link.transactionId %>"
                class="btn btn-warning btn-sm w-100"
              >
                Check Updates
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
    <% } %>
  </div>
</div>

<!-- Admin Modal for Client Details
<div class="modal fade" id="paymentModal" tabindex="-1">
  <div class="modal-dialog modal-lg modal-dialog-centered">
    <div
      class="modal-content shadow-lg rounded-4 overflow-hidden animate__animated animate__zoomIn"
    >
      <div class="modal-header bg-dark text-white">
        <h5 class="modal-title">
          <i class="fas fa-user-shield me-2"></i> Client Details
        </h5>
        <button
          type="button"
          class="btn-close btn-close-white"
          data-bs-dismiss="modal"
        ></button>
      </div>
      <div class="modal-body">
        <div id="paymentDetailsContainer" class="p-2 text-center">
          <p class="text-muted">Select a client to view their details.</p>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  async function loadPaymentDetails(transactionId) {
    const container = document.getElementById("paymentDetailsContainer");
    container.innerHTML = '<p class="text-muted">Loading client details...</p>';

    try {
      const res = await fetch(`/user/link-info/${transactionId}`); // match route exactly
      if (!res.ok) throw new Error("Failed to fetch client details");
      const html = await res.text();
      container.innerHTML = html;
    } catch (err) {
      console.error(err);
      container.innerHTML =
        '<p class="text-danger">❌ Could not load client details.</p>';
    }
  }
</script> -->
